services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      FLASK_SECRET_KEY: dev-secret-key-change-in-production
    volumes:
      - .:/app
      - venv_data:/app/.venv

volumes:
  venv_data:
